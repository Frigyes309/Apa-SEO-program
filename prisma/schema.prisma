// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// model User {
//   id                 Int     @id @default(autoincrement())
//   urlName            String
//   parentDomain       Boolean
//   domainReferer      Boolean
//   domainFullReferer  String
//   domain             String
//   refererRating      Boolean
//   premiumLink        Boolean
//   premiumReferer     Boolean
//   premiumLinkReferer Boolean
//   junkLink           Boolean
// }

model LinkedPage {
  id   Int    @id @default(autoincrement())
  link String
}

model ReferredPageMain {
  id      Int    @id @default(autoincrement())
  refPref String  //wwww. itt van
  dr Int
}

model Domain {
  id        Int @id @default(autoincrement())
  raw       String
  protocol  Boolean //0: https://, 1: http://
  refPrefId Int
  lpId      Int
}
